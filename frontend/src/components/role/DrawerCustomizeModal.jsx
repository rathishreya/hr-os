// Deprecated: the candidate-panel "Customize" feature was removed. The drawer now renders with
// fixed defaults (all tabs, medium width, light overlay, all overview sections, formatted resume
// first) — see CandidateManageDrawer.jsx. This stub remains only so any stale import resolves to
// a harmless no-op instead of breaking the build; it can be deleted once nothing references it.
export default function DrawerCustomizeModal() {
  return null
}
