import JobApplicationsWorkspace from './JobApplicationsWorkspace'

/** Job pipeline — ATS-style applications workspace. */
export default function PipelineView(props) {
  return <JobApplicationsWorkspace {...props} />
}
